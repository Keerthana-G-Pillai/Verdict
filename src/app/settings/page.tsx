import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      subtitle="Configure integrations, API keys, team preferences, and notification rules."
      icon="settings"
    />
  );
}
